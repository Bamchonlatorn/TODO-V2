<p style="color: green"><%= notice %></p>

<% content_for :title, "Categories" %>

<section>
    <h1>Categories 🍂</h1>

    <div>
      <div>
        <% @categories.each do |category| %>
          <div class="category">
            <%= render category %>
            <p>
              <%= link_to "Show this category", category, class: 'link' %>
            </p>
          </div>
        <% end %>
      </div>

      <section>
        <div>
          <%= link_to "New category ✨", new_category_path, class: 'btn' %>
        </div>

        <div> 
          <%= link_to "Back to tasks 🍁", tasks_path, class: 'btn' %>
        </div>
      </section>
    </div>
</section>
